# Wan Music - Docker Compose
# 启动：docker compose up -d
# 访问：http://localhost:6005

services:
  wan-music:
    image: ethanwwan/wan-music:latest
    container_name: wan-music
    restart: unless-stopped
    ports:
      - "6005:6005"
    volumes:
      - ./cookie:/app/clients/cookie   # 持久化 cookie（VIP 登录状态）
